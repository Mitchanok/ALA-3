const menuBar = document.querySelector('.bar');
      const head = document.querySelector('.head');

      menuToggle.addEventListener('click', () => {
        menuToggle.classList.bar('active');
        showcase.classList.bar('active');
      })